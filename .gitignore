# Local-only documentation / design notes (per project preference, not pushed to remote)
docs/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# credentials
.env

# compiled binaries (anchored to repo root so cmd/api/ stays tracked)
/api
/fetcher
