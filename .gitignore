# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# credentials
.env

# claude
CLAUDE.md

# compiled binaries (anchored to repo root so cmd/api/ stays tracked)
/api
/fetcher

# throwaway design mockups & previews (brainstorming only)
/mockups/
.superpowers/

# internal planning docs, specs & presentation prep (kept local, not published)
docs/
